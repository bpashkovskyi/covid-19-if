@{
    ViewData["Title"] = "Covid-19";
}

@model Covid19.ViewModels.ChartsViewModel

<partial name="_Header" for="@Model.Settings" />


@foreach (var chart in Model.Charts)
{
    @(await Html.PartialAsync("_Chart", chart))
}